{%- comment -%}
body-classes.liquid

snippet used to render theme features as classes on the body tag, removed liquid from stylesheet.

theme feature classes (in order)

- product variants (swatches)

theme-features__product-variants--swatches

- section title border

settings.section_title_border

section-title-border

theme-features__section-title-border--

{%- endcomment -%}
{%- liquid

  assign body_classes = ''

  assign base_class = 'theme-features__'


  if template contains 'product'
    assign body_classes = body_classes | append: body_classes | append: ' ' | append: 'product'
  elsif template contains 'collection'
    assign body_classes = body_classes | append: body_classes | append: ' ' | append: 'collection'
  else
    assign body_classes = body_classes | append: body_classes | append: ' ' | append: template | handleize
  endif

  assign body_classes = body_classes | append: ' ' | append: base_class | append: 'product-variants--' | append: settings.product_variants

  assign body_classes = body_classes | append: ' ' | append: base_class | append: 'section-title-border--' | append: settings.section_title_border


  echo body_classes | strip

 -%}
